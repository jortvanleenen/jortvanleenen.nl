<!DOCTYPE html>
<html lang="{{ site.lang }}">
{% include head.liquid %}
<body>
{% include header.liquid %}
<main class="wrapper underline">
    <h2>
        {% if page.title == nil or page.title == empty %}
            {{ page.name | split: "." | first | capitalize }}
        {% else %}
            {{ page.title }}
        {% endif %}
    </h2>
    {{ content }}
</main>
{% include footer.liquid %}
</body>
</html>